<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Class="MyApp.MainWindow"
        Title="Telegram Clone"
        Width="900" Height="600"
        Background="#1f1f1f">

    <Grid ColumnDefinitions="250, *" RowDefinitions="Auto, *">
        <!-- top bar -->
        <Border Grid.ColumnSpan="2" Background="#2b2b2b" Height="50"> 
            <DockPanel Margin="10,0"> 
                <TextBlock Text="Message application" 
                Foreground="White" 
                FontSize="18" 
                VerticalAlignment="Center" 
                DockPanel.Dock="Left"/> 
                </DockPanel> 
                </Border>
        <!-- search bar -->
        <Border Grid.Row="1" Background="#2e2e2e">
            <StackPanel>
                <TextBox Margin="10"
                 Watermark="Tìm kiếm"
                 Background="#3a3a3a"
                 Foreground="White"
                 BorderBrush="#444"
                 CornerRadius="5"/>
            <StackPanel Margin="10,0">
                <!-- contact list -->
            <StackPanel Orientation="Horizontal" Margin="5" Cursor="Hand"
                        >
                <Ellipse Width="40" Height="40" Fill="#4a90e2"/>
                <StackPanel Margin="10,0">
                    <TextBlock Text="Nguyễn Văn A" Foreground="White" FontWeight="Bold"/>
                    <TextBlock Text="Thằng vừng rất ngô" Foreground="#ccc" FontSize="12"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="5" Cursor="Hand"
                        >
                <Ellipse Width="40" Height="40" Fill="#e94e77"/>
                <StackPanel Margin="10,0">
                    <TextBlock Text="Trần Thị B" Foreground="White" FontWeight="Bold"/>
                    <TextBlock Text="Nhớ thằng vừng không?" Foreground="#ccc" FontSize="12"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
                 
            </StackPanel>
            </Border>

        <!-- message box -->
        <Grid Grid.Row="2" Grid.Column="1" Background="#1f1f1f">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ScrollViewer>
                <StackPanel Margin="20">
                    <Border Background="#3a3a3a"
                            CornerRadius="10"
                            Padding="10"
                            HorizontalAlignment="Left"
                            Margin="0,5,50,5">
                        <TextBlock Text="Xin chào!" Foreground="White"/>
                    </Border>

                    <Border Background="#4a90e2"
                            CornerRadius="10"
                            Padding="10"
                            HorizontalAlignment="Right"
                            Margin="50,5,0,5">
                        <TextBlock Text="Chào bạn!" Foreground="White"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>
            <DockPanel Grid.Row="1" Background="#2b2b2b">
                
                <TextBox 
                         Background="#3a3a3a"
                         Foreground="White"
                         BorderBrush="#444"
                         CornerRadius="5"
                         Width="560"
                         Margin="10,6"
                         DockPanel.Dock="Left"
                         Watermark="Nhập tin nhắn..." 
                         HorizontalAlignment="Stretch" />
                <Button Content="Gửi"
                        Background="#4a90e2"
                        Foreground="White"
                        Width=""
                        Padding="10,5"
                        CornerRadius="5"
                        DockPanel.Dock="Right"/>
            </DockPanel>
        </Grid>
    </Grid>
</Window>

